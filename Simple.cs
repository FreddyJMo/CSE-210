using System;

public class Simple : Goals
{
    public Simple(string type, string name, string description, int points) : base (type, name, description, points)
    {
        //Returns the values of the Goals base
    }
}